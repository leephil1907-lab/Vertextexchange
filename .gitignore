# dependencies & builds
node_modules/
dist/

# runtime data (accounts, sessions, tickets — never commit)
server/data.json

# environment & secrets
.env
.env.local
.env.*.local

# logs & OS junk
*.log
npm-debug.log*
.DS_Store
Thumbs.db
