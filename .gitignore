# dependencies & builds (dist/ IS committed — Deno Deploy serves it directly)
node_modules/

# runtime data (accounts, sessions, tickets — never commit)
server/data.json

# environment & secrets
.env
.env.local
.env.*.local

# logs & OS junk
*.log
npm-debug.log*
.DS_Store
Thumbs.db
